---
---
<!DOCTYPE html>
<html>
<head>
  <meta charset="utf-8">
  <meta name="viewport" content="width=device-width,initial-scale=1">
  <meta name="copyright" content="Copyright {{ site.time | date: '%Y' }} {{ site.title }}, All Rights Reserved.">
  <meta name="description" content="{{ site.description }}">
  <meta name="keywords" content="{{ site.keywords }} {{ page.keywords }}">

  <title>
    {% if page.title %}
      {% if site.abbreviation %}
        {{ site.abbreviation }} &middot; {{ page.title }}
      {% else %}
        {{ page.title }}
      {% endif %}
    {% else %}
      {{ site.title }}
    {% endif %}
  </title>

  <link rel="stylesheet" href="{{ site.baseurl }}/styles.css">
  <link rel="preconnect" href="https://fonts.googleapis.com">
  <link rel="preconnect" href="https://fonts.gstatic.com" crossorigin>
  <link href="https://fonts.googleapis.com/css2?family=Lora:wght@400;500&family=Roboto:wght@400;500;700&display=swap" rel="stylesheet">
</head>
<body>
  <nav>
    <a href="{{ site.baseurl }}/">
      <p class="subtitle">Arizona<br />Cactus<br />Quarter<br />Horses</p>
    </a>
    <ul>
      <li><a href="{{ site.baseurl }}/horses">Horses</a></li>
      <li><a href="{{ site.baseurl }}/contact">Contact</a></li>
    </ul>
  </nav>
  {{ content }}
  <p class="fine-print">&copy; {{ site.time | date: '%Y' }} {{ site.title }}, All Rights Reserved.</p>
</body>
</html>
